numbers_list = input("Enter a list of numbers separated by commas: ")
numbers = [int(num) for num in numbers_list.split(",")]

print("Positive numbers in the list:")
for n in numbers:
    if n > 0:
        print(n)
